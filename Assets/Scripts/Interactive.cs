using System.Collections;
using UnityEngine;

public class Interactive : MonoBehaviour
{
    // Use this for initialization
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }

    public virtual void doStuff(string grabbedName)
    {
    }
}